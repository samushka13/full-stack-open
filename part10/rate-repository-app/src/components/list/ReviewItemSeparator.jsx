import { View, StyleSheet } from "react-native";

const styles = StyleSheet.create({
  separator: {
    height: 1,
  },
});

const ReviewItemSeparator = () => <View style={styles.separator} />;

export default ReviewItemSeparator;
